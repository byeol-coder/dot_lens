import { ClassroomModeView } from "@/components/ClassroomModeView";

export const metadata = {
  title: "Classroom Mode | Dot Lens",
  description: "Sync and manage multiple Dot Pads during a live classroom session.",
};

export default function ClassroomPage() {
  return <ClassroomModeView />;
}
