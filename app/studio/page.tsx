import { StudioView } from "@/components/StudioView";

export const metadata = {
  title: "Dot Lens Studio | Dot Lens",
  description: "Create tactile lessons from visual materials. Import from TIB.",
};

export default function StudioPage() {
  return <StudioView />;
}
